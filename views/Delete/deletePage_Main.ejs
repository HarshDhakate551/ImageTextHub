<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../particles/link') %>
    <title>Delete Page</title>
</head>
<body>
    <nav class="navbar  navbar-expand-lg" style="background-color: #e3f2fd;">
        <div class="container-fluid">
          <span class="navbar-brand mb-0 h1" style="font-size: 300%;">Delete Page</span>
        </div>
      </nav>
      <hr />

      <nav class="nav nav-pills flex-column flex-sm-row">
          <a class="flex-sm-fill text-sm-center nav-link" aria-current="page" href="/">Home</a>
          <a class="flex-sm-fill text-sm-center nav-link" aria-current="page" href="/show">Views</a>
          <a class="flex-sm-fill text-sm-center nav-link" href="/upload">Upload</a>
          <a class="flex-sm-fill text-sm-center nav-link active" href="/delete">Delete</a>
          <a class="flex-sm-fill text-sm-center nav-link" href="/update">Update</a>
      </nav>

      <hr/>
      <h2 style="background-color : yellow;">It's compulsory for Data To present before Delete else Server Crashes </h2>
      <form action="/Delete_Perticular_Data_PAGE" method="get">
          <div style="display: flex; align-items: center; justify-content: center;">
              <div class="row g-3 align-items-center">
                  <div class="col-auto">
                      <label for="No" class="col-form-label">Enter SrNo. :</label>
                </div>
                <div class="col-auto">
                    <input type="number" id="No" class="form-control" name="No" min="1" />
                </div>
                <div class="col-auto">
                    <span id="SrNo" class="form-text">
                        Must be greater than 1.
                    </span>
                </div>
            </div>
            <div style="margin-left: 10px;">
                <button class="btn btn-outline-primary">Delete</button>   
            </div>
        </div>
    </form>
    <br/> 
    <div style="display: flex; justify-content: center; align-items: center;">
        <a class="btn btn-outline-warning" href="/DeleteAll_Data_Page">Delete All</a>
    </div>
    <br />
    <br/> 

    <div>      
        <% if (datas.length === 0) { %>
            <h1>No Quotes found in the database.</h1>
            <% } else { %>
                <h1>Quotes</h1>
            
                <% for (const data of datas) { %>
                <% if (!data.quote) { %>
                    <p>No quote available for this entry.</p>
                <% } else { %>
                    <h3>
                        <span><%= data.SrNo %>.</span>
                        <span><%= data.quote %></span>
                    </h3>
                    <% } %>
                    <% } %>
                    <% } %>
                    
    </div>
        <br />
                    
        <%- include('../particles/script') %>
    </body>
</html>