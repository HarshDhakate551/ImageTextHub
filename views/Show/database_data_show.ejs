<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('../particles/link') %>
  <title>View Quotes</title>
</head>
<body>
  <nav class="navbar  navbar-expand-lg" style="background-color: #e3f2fd;">
    <div class="container-fluid">
      <span class="navbar-brand mb-0 h1" style="font-size: 300%;">View Page</span>
    </div>
  </nav>
  <hr />
  
  <nav class="nav nav-pills flex-column flex-sm-row">
    <a class="flex-sm-fill text-sm-center nav-link" aria-current="page" href="/">Home</a>
    <a class="flex-sm-fill text-sm-center nav-link active" aria-current="page" href="/show">Views</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/upload">Upload</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/delete">Delete</a>
    <a class="flex-sm-fill text-sm-center nav-link" href="/update">Update</a>
  </nav>
  
<hr style="height: 10px;">
  <% if (datas.length === 0) { %>
    <h1>No Quotes found in the database.</h1>
    <% } else { %>
      <h1>Quotes</h1>
      <ul>
        <% for (const data of datas) { %>
          <% if (!data.quote) { %>
          <li>No quote available for this entry.</li>
        <% } else { %>
          <li><%= data.quote %></li>
          <% } %>
          <% } %>
          <% } %>
  </ul>
  <br />

  
  <%- include('../particles/script') %>
</body>
</html>